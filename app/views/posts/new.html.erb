<% title "Neuen Beitrag für " + @post.discussion.title %>

<h3>Neuer Beitrag für <strong><%= @post.discussion.title %></strong></h3>

<%= render 'form' %>

<%= render partial: "discussions/show_discussion", locals: { discussion: @post.discussion } %>